
blur: {
    method: "gaussian";
    size: 60;
    deviation: 5.0;
}

opacity-rule = [ "80:class_g = 'Alacritty'" ];
opacity-rule = [
  "85:class_g = 'Zathura'",
  "85:class_g = 'Alacritty'"
];

backend = "glx";
glx-no-stencil = true;
glx-no-rebind-pixmap = true;

vsync = true;
